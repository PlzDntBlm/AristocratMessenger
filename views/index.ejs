<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Aristocrat Messenger</title>
    <link rel="stylesheet" href="/css/output.css">
    <style>
        #content {
            /* border: 1px dashed #ccc; */ /* Can remove this now */
            padding: 20px;
            min-height: 300px; /* Keep a min-height for visual consistency */
            margin-top: 20px;
        }
    </style>
</head>
<body class="bg-gray-100 font-sans">
<header class="bg-blue-600 text-white p-4">
    <div class="container mx-auto flex justify-between items-center">
        <h1 class="text-2xl font-bold">Aristocrat Messenger</h1>
        <nav>
            <% if (isLoggedIn) { %>
                <a href="#home" class="text-white hover:text-gray-300 px-3 py-2 rounded-md text-sm font-medium" data-partial="home">Home</a>
                <span class="text-gray-300 px-3 py-2 text-sm">Welcome, <%= username %>!</span>
                <button id="logout-button" class="bg-red-500 hover:bg-red-700 text-white font-bold py-1 px-3 rounded text-sm">Logout</button>
            <% } else { %>
                <a href="#login" class="text-white hover:text-gray-300 px-3 py-2 rounded-md text-sm font-medium" data-partial="login">Login</a>
                <a href="#register" class="text-white hover:text-gray-300 px-3 py-2 rounded-md text-sm font-medium" data-partial="register">Register</a>
            <% } %>
        </nav>
    </div>
</header>

<main id="content" class="container mx-auto mt-5 p-5 bg-white rounded shadow">
    <p class="text-center text-gray-500">Initializing...</p>
</main>

<footer class="text-center mt-10 text-gray-600 text-sm">
    <p>&copy; 2025 a ChrisChris Experience</p>
</footer>

<script type="module" src="/js/app.js"></script>
</body>
</html>